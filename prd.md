# 웹 메모장 앱 — Product Requirements Document (PRD)

- 문서 버전: v1.0
- 작성일: 2026-09-23
- 작성자: papasmf1
- 상태: Draft (구현 전 기획 단계)

---

## 1. 개요

### 1.1 제품명 (가칭)
Web Notepad (메모장)

### 1.2 한 줄 정의
웹 브라우저에서 로그인 없이 또는 로그인 후 빠르게 메모를 작성·정리·검색할 수 있는 가볍고 반응 빠른 온라인 메모장.

### 1.3 배경 및 문제 정의
- 사용자는 짧은 생각, 할 일, 링크, 코드 스니펫 등을 즉시 기록할 도구가 필요하다.
- 기존 노트 앱(Notion, Evernote 등)은 기능이 무겁고 로딩이 느리며, 간단한 메모 하나를 남기기 위한 진입 장벽이 높다.
- OS 기본 메모장은 기기 간 동기화가 되지 않거나 브라우저 환경에서 쓸 수 없다.
- "브라우저만 열면 바로 쓸 수 있는" 가볍고 빠른 메모 도구에 대한 니즈가 존재한다.

### 1.4 목표
- 브라우저 접속 후 3초 이내에 새 메모 작성을 시작할 수 있어야 한다.
- 메모 작성/저장/검색이 끊김 없이(즉각적으로) 동작해야 한다.
- 데스크톱과 모바일 브라우저 모두에서 동일한 핵심 경험을 제공해야 한다.

### 1.5 목표가 아닌 것 (Non-Goals)
- 실시간 공동 편집(여러 사용자가 동시에 같은 메모를 편집)은 v1 범위 밖.
- 오프라인 네이티브 앱(iOS/Android 전용 앱) 제작은 범위 밖.
- 고급 리치 텍스트(표, 이미지 삽입, 파일 첨부)는 v1 범위 밖 — Post-MVP 후보.
- 팀/조직 단위 협업 기능(권한 관리, 공유 워크스페이스)은 범위 밖.

---

## 2. 타깃 사용자 및 페르소나

### 2.1 주요 타깃
- 개인 사용자로서 아이디어, 할 일, 임시 메모를 빠르게 기록하고 싶은 사람.
- 여러 기기(회사 PC, 개인 노트북, 스마트폰 브라우저)에서 같은 메모에 접근하고 싶은 사람.
- 개발자, 학생, 직장인 등 텍스트 기반 메모를 자주 남기는 사용자.

### 2.2 페르소나 예시
**페르소나 A — "민준", 32세, 백엔드 개발자**
- 작업 중 떠오르는 TODO, 커맨드, 링크를 즉시 기록하고 싶다.
- 마크다운 문법으로 코드 블록을 표현할 수 있으면 좋겠다.
- 회사 PC와 집 노트북에서 같은 메모를 이어서 보고 싶다.

**페르소나 B — "지현", 27세, 대학원생**
- 강의 중 노트를 빠르게 작성하고 태그로 과목별 분류를 하고 싶다.
- 나중에 특정 키워드로 과거 메모를 검색해서 찾고 싶다.

---

## 3. 사용자 스토리 (User Stories)

우선순위: P0(필수) / P1(중요) / P2(향후)

| ID | 우선순위 | 사용자 스토리 |
|----|---------|---------------|
| US-01 | P0 | 사용자로서, 새 메모를 클릭 한 번으로 생성하고 싶다. |
| US-02 | P0 | 사용자로서, 메모 제목과 본문을 입력하면 자동으로 저장되어 저장 버튼을 누를 필요가 없으면 좋겠다. |
| US-03 | P0 | 사용자로서, 작성한 메모 목록을 최신순으로 한눈에 보고 싶다. |
| US-04 | P0 | 사용자로서, 특정 메모를 클릭해 상세 내용을 열람/수정하고 싶다. |
| US-05 | P0 | 사용자로서, 불필요한 메모를 삭제하고 싶다. |
| US-06 | P0 | 사용자로서, 제목/본문 키워드로 메모를 검색하고 싶다. |
| US-07 | P1 | 사용자로서, 계정을 만들어 로그인하면 여러 기기에서 내 메모에 접근하고 싶다. |
| US-08 | P1 | 사용자로서, 메모에 태그를 붙여 분류하고 싶다. |
| US-09 | P1 | 사용자로서, 태그나 카테고리로 메모 목록을 필터링하고 싶다. |
| US-10 | P1 | 사용자로서, 메모를 즐겨찾기(고정)하여 목록 상단에 고정하고 싶다. |
| US-11 | P1 | 사용자로서, 실수로 삭제한 메모를 일정 기간 내에 복구하고 싶다(휴지통). |
| US-12 | P1 | 사용자로서, 다크 모드/라이트 모드를 전환하고 싶다. |
| US-13 | P2 | 사용자로서, 메모를 마크다운으로 작성하고 미리보기로 렌더링된 결과를 보고 싶다. |
| US-14 | P2 | 사용자로서, 메모를 다른 사람과 읽기 전용 링크로 공유하고 싶다. |
| US-15 | P2 | 사용자로서, 메모를 텍스트/마크다운 파일로 내보내고 싶다. |
| US-16 | P2 | 사용자로서, 키보드 단축키로 새 메모 생성/저장/검색을 빠르게 실행하고 싶다. |

---

## 4. 기능 요구사항 (Functional Requirements)

### 4.1 메모 CRUD (P0)
- **생성**: "새 메모" 버튼 또는 단축키로 빈 메모를 즉시 생성한다. 생성 즉시 편집 화면으로 이동한다.
- **조회**: 좌측(또는 상단) 목록에서 메모 리스트를 확인한다. 목록에는 제목, 본문 일부 미리보기, 최종 수정 시각이 표시된다.
- **수정**: 사용자가 타이핑을 멈추면 일정 시간(예: 500ms~1s) 후 자동 저장된다. 저장 상태(저장 중/저장됨)를 시각적으로 표시한다.
- **삭제**: 목록 또는 상세 화면에서 삭제할 수 있다. 삭제 시 확인 다이얼로그를 표시한다. (P1: 즉시 영구 삭제 대신 휴지통으로 이동)

### 4.2 목록 및 정렬 (P0)
- 기본 정렬은 "최근 수정순".
- 정렬 옵션: 최근 수정순, 생성일순, 제목순(가나다).
- 메모가 없을 경우 빈 상태(Empty State) UI를 안내 문구와 함께 표시한다.

### 4.3 검색 (P0)
- 검색창에 키워드 입력 시 제목/본문에 포함된 메모를 실시간(디바운스 적용)으로 필터링한다.
- 검색 결과가 없을 경우 안내 메시지를 표시한다.

### 4.4 사용자 인증 (P1)
- 이메일/비밀번호 기반 회원가입 및 로그인.
- 비로그인 상태에서도 임시로 메모를 작성할 수 있되(로컬 저장), 로그인 시 서버로 마이그레이션할지는 정책 결정 필요(§9 참고).
- 로그아웃 기능 제공.
- 로그인한 사용자는 본인이 작성한 메모만 조회/수정/삭제 가능(데이터 격리).

### 4.5 태그 / 분류 (P1)
- 메모 하나에 여러 개의 태그를 추가할 수 있다.
- 태그 목록을 사이드바 등에서 확인하고 클릭 시 해당 태그가 달린 메모만 필터링한다.

### 4.6 고정(Pin) (P1)
- 메모를 고정하면 목록 최상단의 "고정됨" 섹션에 표시된다.

### 4.7 휴지통 (P1)
- 삭제된 메모는 30일간 휴지통에 보관 후 자동 영구 삭제된다.
- 휴지통에서 복구 또는 영구 삭제를 선택할 수 있다.

### 4.8 다크/라이트 모드 (P1)
- 시스템 설정을 따르는 기본값 + 사용자가 수동으로 전환 가능한 토글 제공.

### 4.9 마크다운 지원 (P2)
- 본문 입력 시 마크다운 문법 지원(굵게, 기울임, 목록, 코드블록, 링크 등).
- 편집/미리보기 모드 전환 또는 실시간 렌더링.

### 4.10 공유 (P2)
- 특정 메모에 대해 "읽기 전용 공유 링크"를 생성할 수 있다.
- 공유 링크 비활성화(회수) 기능 제공.

### 4.11 내보내기 (P2)
- 메모를 `.txt` 또는 `.md` 파일로 다운로드.

### 4.12 단축키 (P2)
- 예: `Ctrl/Cmd + N` 새 메모, `Ctrl/Cmd + K` 검색, `Ctrl/Cmd + Backspace` 삭제 등.

---

## 5. 비기능 요구사항 (Non-Functional Requirements)

| 구분 | 요구사항 |
|------|----------|
| 성능 | 초기 페이지 로드(LCP) 2.5초 이내(4G 기준). 메모 목록 100개 기준 스크롤/검색 지연 없음. |
| 반응형 | 데스크톱(≥1024px), 태블릿(≥768px), 모바일(<768px) 레이아웃 대응. |
| 접근성 | WCAG 2.1 AA 수준 준수 목표(키보드 내비게이션, 명도 대비, 스크린리더 라벨). |
| 브라우저 지원 | 최신 Chrome, Edge, Safari, Firefox (최근 2개 메이저 버전). |
| 데이터 무결성 | 자동 저장 실패 시 사용자에게 명확한 오류 알림 및 재시도 제공. |
| 보안/개인정보 | 사용자는 본인 메모만 접근 가능해야 한다(인증/인가). 비밀번호는 평문 저장 금지. |
| 가용성 | 메모 작성/조회 관련 핵심 기능 가용성 99.5% 목표(운영 단계 기준). |
| 국제화 | v1은 한국어 UI 우선, 추후 영어 등 다국어 확장 가능하도록 문자열 분리 고려. |

---

## 6. 정보 구조 및 핵심 화면

### 6.1 주요 화면 목록
1. **메모 목록 화면**: 전체 메모 리스트, 검색창, 새 메모 버튼, 태그 필터.
2. **메모 상세/편집 화면**: 제목, 본문, 태그, 저장 상태, 삭제/고정 버튼.
3. **로그인/회원가입 화면** (P1).
4. **휴지통 화면** (P1).
5. **설정 화면**: 다크모드 전환, 계정 정보, 로그아웃 (P1).

### 6.2 레이아웃 형태
- 데스크톱: 좌측 목록 패널 + 우측 편집 패널의 2-패널(마스터-디테일) 레이아웃.
- 모바일: 목록 화면과 편집 화면을 별도 라우트/화면으로 전환하는 1-패널 방식.

### 6.3 핵심 사용자 흐름 (Happy Path)
```
[진입] → [새 메모 클릭] → [제목/본문 입력] → [자동 저장]
   → [목록으로 돌아가기] → [검색 또는 목록에서 메모 재열람]
   → [필요 시 수정/삭제/고정]
```

---

## 7. 성공 지표 (Success Metrics)

| 지표 | 목표 |
|------|------|
| 신규 메모 생성까지 걸리는 시간 | 페이지 진입 후 3초 이내 |
| 자동 저장 성공률 | 99.9% 이상 |
| 검색 응답 체감 속도 | 입력 후 300ms 이내 결과 표시 |
| 주간 재방문율 (WAU/MAU) | 출시 후 3개월 내 30% 이상 (로그인 사용자 기준) |
| 사용자당 평균 메모 수 | 출시 후 1개월 내 5개 이상 |

---

## 8. 마일스톤 (제안)

| 단계 | 범위 | 포함 스토리 |
|------|------|-------------|
| M1 — MVP | 로컬/단일 사용자 기준 메모 CRUD, 목록, 검색 | US-01 ~ US-06 |
| M2 — 계정/동기화 | 로그인, 사용자별 데이터 격리, 태그, 고정, 휴지통, 다크모드 | US-07 ~ US-12 |
| M3 — 확장 기능 | 마크다운, 공유, 내보내기, 단축키 | US-13 ~ US-16 |

---

## 9. 열린 이슈 / 결정 필요 사항

1. **비로그인 사용자의 메모 저장 위치**: 브라우저 로컬 저장(LocalStorage)만 지원할지, 로그인 시 서버로 이전(마이그레이션)할지 정책 결정 필요.
2. **휴지통 보관 기간**: 30일을 기본값으로 제안했으나 확정 필요.
3. **메모 최대 글자 수 제한**: 성능/DB 컬럼 제약을 고려해 상한선 설정 필요(예: 100,000자).
4. **공유 링크의 만료 정책**: 무기한 vs. 기간제(예: 7일) 중 선택 필요.
5. **소셜 로그인(Google 등) 지원 여부**: v1 범위 포함 여부 결정 필요.

---

## 10. 용어 정의

| 용어 | 정의 |
|------|------|
| 메모(Note) | 사용자가 작성하는 제목 + 본문으로 구성된 텍스트 단위 데이터. |
| 고정(Pin) | 메모를 목록 상단에 우선 노출시키는 상태. |
| 휴지통(Trash) | 삭제된 메모가 영구 삭제 전 임시 보관되는 영역. |
| 자동 저장(Autosave) | 사용자의 명시적 저장 액션 없이 입력 중단 시 자동으로 서버/저장소에 반영되는 동작. |
